export class Car{
    model: string; 
    plate: string;
    year: string;
    state: number;
    constructor(model, plate, year, state){
        this.model=model;
        this.plate=plate;
        this.year=year;
        this.state=state;
    }
}